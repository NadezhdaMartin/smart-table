import { defineConfig } from 'vite'

export default defineConfig({
  // Замените на имя вашего репозитория, если это не username.github.io
  // Если это username.github.io, оставьте base: '/' или удалите эту строку
  base: '/smart-table/', 
})